name: Replace repository with N Production Final

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  replace:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Extract production ZIP
        shell: bash
        run: |
          set -e

          ZIP="N-production-final-3.0.0.zip"

          test -f "$ZIP"

          rm -rf /tmp/n_final
          mkdir -p /tmp/n_final

          unzip -q "$ZIP" -d /tmp/n_final

          ROOT="/tmp/n_final"

          if [ -d "$ROOT/N_complete_release" ]; then
            ROOT="$ROOT/N_complete_release"
          fi

          test -f "$ROOT/pubspec.yaml"
          test -d "$ROOT/lib"
          test -d "$ROOT/android"

          find . -mindepth 1 -maxdepth 1 ! -name ".git" -exec rm -rf {} +

          cp -a "$ROOT"/. .

          rm -f "$ZIP"

          git add -A

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git commit -m "Replace repository with N Production Final"
          git push origin HEAD:main
