name: Clean Replace N Production

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  replace:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Clean and replace
        shell: bash
        run: |
          set -e

          ZIP="N-production-full-4.0.0.zip"

          test -f "$ZIP"

          rm -rf /tmp/n_project
          mkdir -p /tmp/n_project

          unzip -q "$ZIP" -d /tmp/n_project

          ROOT="/tmp/n_project"

          if [ -d "$ROOT/N_complete_release" ]; then
            ROOT="$ROOT/N_complete_release"
          fi

          test -f "$ROOT/pubspec.yaml"
          test -d "$ROOT/lib"
          test -d "$ROOT/android"

          # حذف كل محتوى المستودع القديم مع إبقاء Git
          find . -mindepth 1 -maxdepth 1 ! -name ".git" -exec rm -rf {} +

          # نسخ المشروع الجديد بالكامل
          cp -a "$ROOT"/. .

          # حذف ملف ZIP من المشروع
          rm -f "$ZIP"

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add -A

          git commit -m "Clean replace with N Production Full" || exit 0

          git push origin HEAD:main
